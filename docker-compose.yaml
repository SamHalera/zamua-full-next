services:
  postgres:
    image: postgres:15-alpine
    ports:
      - 5433:5432 # enables connecting locally via psql
    environment:
      POSTGRES_DB: zamua-full-next-db
      POSTGRES_PASSWORD: nigutenisawa
      POSTGRES_USER: samdev
